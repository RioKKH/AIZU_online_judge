#include <stdio.h>
#include <iostream>

int main(int argc, char const* argv[])
{
	int H, W;

	while(true) {
		std::cin >> H >> W;
		if (H == 0 && W ==0) break;
		for (int h = 0; h < H; h++) {
			for (int w = 0; w < W; w++) {
	
	return 0;
}
